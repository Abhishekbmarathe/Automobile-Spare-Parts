<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #000; /* Black background for the body */
        }
        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background-color: #000; /* Black background for the container */
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(255,255,255,0.1); /* Subtle white shadow */
            color: #fff; /* White text color for general readability */
        }
        h1 {
            color: #d10000; /* Red color for headers */
            text-align: center;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            
        }
        th, td {
            border: 1px solid #fff; /* White borders for cells */
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #d10000; /* Red header background */
            color: #ffffff; /* White text for contrast */
        }
        td {
            color: #fff; /* White text for content */
        }
        .fulfill-btn {
            padding: 5px 10px;
            background-color: #d10000; /* Red background for buttons */
            border: none;
            border-radius: 5px;
            color: #fff; /* White text */
            cursor: pointer;
        }
        .fulfill-btn:hover {
            background-color: #a30000; /* Darker red on hover */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Order Details</h1>
        <table>
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Phone</th>
                    <th>Address</th>
                    <th>Item Name</th>
                    <th>Price</th>
                    <th>payment-type</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% orderDetails.forEach(detail => { %>
                    <tr>
                        <td><%= detail.name %></td>
                        <td><%= detail.email %></td>
                        <td><%= detail.phone %></td>
                        <td><%= detail.address %></td>
                        <td><%= detail.itemName ? detail.itemName : 'N/A' %></td>
                        <td><%= detail.price ? detail.price : 'N/A' %></td>
                        <td><%= detail.payment %></td>
                        <td><button class="fulfill-btn" onclick="markFulfilled('<%= detail._id %>')">Fulfilled</button></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
    <script>
        function markFulfilled(orderId) {
            // Example: Send a request to the server to update the order status
            console.log("Mark order as fulfilled: ", orderId);
            // You can use fetch to call a server endpoint here
        }
    </script>
</body>
</html>
